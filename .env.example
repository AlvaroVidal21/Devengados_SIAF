# Token de autorización para la API de SIAF (MEF)
# (Requerido solo si ejecutas los scripts de prueba en playground/)
# El extractor principal (extractor.py) se autentica mediante Playwright y no necesita este token.
SIAF_TOKEN=tu_token_aqui_sin_comillas
